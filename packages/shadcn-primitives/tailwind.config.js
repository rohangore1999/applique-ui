/**
 * Tailwind 4 is CSS-first.
 *
 * The package theme, source boundaries and semantic tokens live in
 * `src/design.css`. This empty CommonJS export remains only for older tooling
 * that probes for a Tailwind config file; the CSS and catalog builds do not
 * load it.
 */
module.exports = {}
